import 'package:flutter/material.dart';

class ParticipanteHomeView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text("PARTICIPANTE HOME"),
        ),
      ),
    );
  }
}
